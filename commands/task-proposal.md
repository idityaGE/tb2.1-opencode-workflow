---
description: Research a hard TB2 task idea, prepare proposal fields, iterate on platform checks, and build only after approval.
agent: tb2-task-proposer
---

# /task-proposal

Research and prepare one extremely hard Terminal-Bench 2 task proposal without creating task files until the proposal passes the platform checks and the user approves it.

User arguments: `$ARGUMENTS`

Follow the `tb2-task-proposer` agent instructions as the workflow source of truth. Pass the user arguments through as constraints and return the agent's required response shapes.
